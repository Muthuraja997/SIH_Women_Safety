name: sih_women_safety
description: "A new Flutter project."
#kkl
#helo
# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  firebase_auth: ^5.2.1
  firebase_core: ^3.4.1
  firebase_messaging: ^15.1.1
  flutter:
    sdk: flutter
  flutter_core_httpproxy: ^1.0.1
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
